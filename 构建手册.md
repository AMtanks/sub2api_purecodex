# Sub2API PureCodex 双端搭建手册

本文档对应下面这套部署形态：

- 前端：Vercel
- 后端：Ubuntu 服务器
- 数据库：服务器本机 Docker PostgreSQL
- Redis：服务器本机 Docker Redis
- 反向代理：服务器本机 Nginx

推荐域名规划：

- 前端：`https://app.example.com`
- 后端：`https://api.example.com`

不要把前端和后端都挂在同一个低内存服务器里重新构前端。这个仓库现在已经支持：

- Vercel 单独部署前端
- 服务器只构建 Go 后端
- 前端通过 `VITE_API_BASE_URL` 指向后端
- 后台 OAuth 回调地址建议值优先使用后端域名

---

## 1. 部署前准备

### 1.1 DNS 规划

先在你的 DNS 提供商里添加两条记录：

- `app.example.com` 指向 Vercel
- `api.example.com` 指向你的 Ubuntu 服务器公网 IP

如果你使用 Cloudflare：

- `app.example.com` 可以按 Vercel 官方接入方式配置
- `api.example.com` 可以继续走 Cloudflare 代理

---

## 2. 服务器部署后端

以下步骤默认服务器是全新 Ubuntu，且目标目录为 `/opt/sub2api_purecodex`。

### 2.1 克隆代码

```bash
git clone https://github.com/AMtanks/sub2api_purecodex.git /opt/sub2api_purecodex
cd /opt/sub2api_purecodex
```

如果目录已经存在，不要重复 `git clone`，改用：

```bash
cd /opt/sub2api_purecodex
git pull
```

### 2.2 首次安装系统依赖并初始化目录

这里仍然复用仓库自带脚本装 Docker、Nginx 和基础目录，但跳过 Tailscale。

```bash
cd /opt/sub2api_purecodex

export PRIMARY_DOMAIN="api.example.com"
export EXTRA_SERVER_NAMES=""
export SUB2API_ADMIN_PASSWORD="请替换成你的管理员密码"
export SKIP_TAILSCALE="true"

bash deploy/remote-first-run.sh
```

说明：

- `PRIMARY_DOMAIN` 这里填后端域名，不是前端域名
- `SKIP_TAILSCALE=true` 会跳过 Tailscale 安装/拉起
- 这一步会生成 `deploy/.env`、拉起数据库和 Redis

### 2.3 改用后端专用 compose

双端部署不应该继续用会构建前端的 `docker-compose.build.yml`。

进入部署目录：

```bash
cd /opt/sub2api_purecodex/deploy
```

停掉旧容器：

```bash
docker compose -f docker-compose.build.yml down
```

使用后端专用构建：

```bash
docker compose -f docker-compose.backend-only.yml up -d --build
```

说明：

- 这个 compose 使用 [deploy/docker-compose.backend-only.yml](G:/sub2api/sub2api_purecodex/deploy/docker-compose.backend-only.yml:1)
- 它对应的镜像构建文件是 [Dockerfile.backend-only](G:/sub2api/sub2api_purecodex/Dockerfile.backend-only:1)
- 它不会在服务器里构建前端

### 2.4 检查后端是否正常

```bash
cd /opt/sub2api_purecodex/deploy

docker ps
docker compose -f docker-compose.backend-only.yml logs --tail=200
curl http://127.0.0.1:8080/health
```

如果健康检查正常，应该能看到 HTTP 200 或健康 JSON。

---

## 3. 配置后端跨域与前端地址

双端部署必须补两类配置：

- 后端允许 Vercel 前端跨域访问
- 后端知道前端公网地址，用于邮件链接等场景

### 3.1 准备 `config.yaml`

如果你还没有自定义配置文件：

```bash
cd /opt/sub2api_purecodex/deploy
cp config.example.yaml config.yaml
```

编辑：

```bash
nano config.yaml
```

至少修改这些字段：

```yaml
server:
  frontend_url: "https://app.example.com"

cors:
  allowed_origins:
    - "https://app.example.com"
  allow_credentials: true
```

说明：

- `server.frontend_url` 用于密码重置邮件、外链等前端回跳场景
- `cors.allowed_origins` 必须包含你的 Vercel 前端正式域名
- 如果你还有 Vercel preview 域名要调试，可以临时再加一个 preview 域名

### 3.2 把配置挂进容器

编辑 `deploy/docker-compose.backend-only.yml`，在 `sub2api` 的 `volumes` 下增加：

```yaml
      - ./config.yaml:/app/data/config.yaml
```

如果你想直接改文件，也可以参考完整段落：

```yaml
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/data/config.yaml
```

保存后重启：

```bash
cd /opt/sub2api_purecodex/deploy
docker compose -f docker-compose.backend-only.yml up -d --build
```

### 3.3 再次检查后端

```bash
cd /opt/sub2api_purecodex/deploy
docker compose -f docker-compose.backend-only.yml logs --tail=200
curl http://127.0.0.1:8080/health
```

---

## 4. 配置 Nginx 暴露后端域名

如果你已经通过 `remote-first-run.sh` 生成过 Nginx 配置，可以先检查：

```bash
sudo nginx -t
sudo systemctl status nginx --no-pager
```

如果你需要手工确认反代目标，核心应该是：

- `api.example.com` -> `127.0.0.1:8080`

修改后执行：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

再从服务器本机检查：

```bash
curl -I https://api.example.com/health
curl -I https://api.example.com/api/v1/settings/public
```

---

## 5. 在 Vercel 部署前端

这部分默认你在本地开发机操作，不在 Ubuntu 服务器上做。

### 5.1 准备 Vercel CLI

```bash
npm i -g vercel
vercel login
```

### 5.2 进入前端目录

```bash
cd G:\sub2api\sub2api_purecodex\frontend
```

### 5.3 绑定 Vercel 项目

首次执行：

```bash
vercel
```

如果你要显式绑定已有项目：

```bash
vercel link --yes
```

### 5.4 配置前端环境变量

必须配置：

```bash
vercel env add VITE_API_BASE_URL production
```

输入值：

```bash
https://api.example.com/api/v1
```

如果你还要让 preview 环境也可用：

```bash
vercel env add VITE_API_BASE_URL preview
```

同样输入：

```bash
https://api.example.com/api/v1
```

如果你想拉到本地 `.env`：

```bash
vercel env pull .env.local
```

### 5.5 本地构建验证前端

仓库已经新增了 `build:vercel`：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
```

说明：

- `build:vercel` 会把产物输出到 `frontend/dist`
- [frontend/vercel.json](G:/sub2api/sub2api_purecodex/frontend/vercel.json:1) 已经补了 SPA rewrite
- [frontend/vite.config.ts](G:/sub2api/sub2api_purecodex/frontend/vite.config.ts:39) 已支持通过 `VITE_BUILD_OUT_DIR` 切换产物目录

### 5.6 部署到 Vercel

预览部署：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel
```

正式部署：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel --prod
```

---

## 6. 首次进入后台后的必须配置

部署完成后，先打开：

- 前端：`https://app.example.com`
- 后端公开接口：`https://api.example.com/api/v1/settings/public`

登录后台后，进入系统设置，至少确认下面几项：

### 6.1 Site / OEM

- `api_base_url` 填：

```text
https://api.example.com/api/v1
```

- `frontend_url` 填：

```text
https://app.example.com
```

说明：

- 现在前端代码优先使用 `VITE_API_BASE_URL`
- 后台 `api_base_url` 仍然建议填好，给用户复制 API 地址、以及运行时回退使用

### 6.2 OAuth callback 地址

现在后台里“自动生成回调地址”的建议值会优先使用后端域名。

你应该确认这些后端回调地址都指向 `api.example.com`：

- LinuxDo:

```text
https://api.example.com/api/v1/auth/oauth/linuxdo/callback
```

- GitHub:

```text
https://api.example.com/api/v1/auth/oauth/github/callback
```

- Google:

```text
https://api.example.com/api/v1/auth/oauth/google/callback
```

- WeChat:

```text
https://api.example.com/api/v1/auth/oauth/wechat/callback
```

- OIDC:

```text
https://api.example.com/api/v1/auth/oauth/oidc/callback
```

前端回跳地址则继续保持前端域名下的页面路径，例如：

```text
/auth/oauth/callback
/auth/wechat/callback
/auth/oidc/callback
```

---

## 7. 双端部署完成后的验证清单

### 7.1 后端检查

```bash
curl https://api.example.com/health
curl https://api.example.com/api/v1/settings/public
```

### 7.2 前端检查

打开：

```text
https://app.example.com
```

确认：

- 首页能打开
- 登录页能打开
- 浏览器控制台没有大面积 CORS 报错
- 登录请求发往 `https://api.example.com/api/v1/...`

### 7.3 OAuth 检查

至少点一次你启用的 OAuth 登录按钮，确认跳转出去的地址是：

```text
https://api.example.com/api/v1/auth/oauth/...
```

而不是：

```text
https://app.example.com/api/v1/auth/oauth/...
```

---

## 8. 后续更新流程

### 8.1 更新后端

在服务器执行：

```bash
cd /opt/sub2api_purecodex
git pull

cd /opt/sub2api_purecodex/deploy
docker compose -f docker-compose.backend-only.yml up -d --build
docker compose -f docker-compose.backend-only.yml logs --tail=200
```

### 8.2 更新前端

在本地执行：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
vercel --prod
```

---

## 9. 常见问题

### 9.1 前端请求打到了 `app.example.com/api/v1`

原因通常是 `VITE_API_BASE_URL` 没配。

检查：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel env ls
```

确认存在：

```text
VITE_API_BASE_URL=https://api.example.com/api/v1
```

### 9.2 浏览器报 CORS

检查后端 `config.yaml`：

```yaml
cors:
  allowed_origins:
    - "https://app.example.com"
  allow_credentials: true
```

改完后重启：

```bash
cd /opt/sub2api_purecodex/deploy
docker compose -f docker-compose.backend-only.yml up -d --build
```

### 9.3 OAuth 回调地址被填成前端域名

现在代码里建议值已经优先读后端地址，但如果你后台历史配置里已经存错了，仍要手工改回：

```text
https://api.example.com/api/v1/auth/oauth/<provider>/callback
```

### 9.4 服务器还在尝试构建前端

说明你跑错了 compose。

应该使用：

```bash
docker compose -f docker-compose.backend-only.yml up -d --build
```

不要再用：

```bash
docker compose -f docker-compose.build.yml up -d --build
```

因为后者仍然会构建前端。

