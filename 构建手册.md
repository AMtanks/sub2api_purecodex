Sub2API PureCodex 构建手册
本文档只保留当前最适合 1C1G 服务器的上线方案：

后端：继续使用官方镜像部署到 Ubuntu 服务器
前端：部署到 Vercel
不在服务器上做任何源码编译
适用前提：

网站主域名：purecodex.asia
前端域名：https://purecodex.asia
前端备用域名：https://www.purecodex.asia
后端域名：https://api.purecodex.asia
服务器公网 IP：45.61.135.12
这份手册的目标是：只走你已经验证成功的路径，不再使用本仓库源码在服务器上构建 Docker 镜像。

总体架构
最终流量关系：
用户访问：https://purecodex.asia
由 Vercel 提供前端页面
前端通过 https://api.purecodex.asia/api/v1 调后端 API
后端运行在你的 Ubuntu 服务器 45.61.135.12
一句话总结：

purecodex.asia = Vercel 前端
api.purecodex.asia = Ubuntu 后端
2. 为什么不用源码编译部署
你的服务器是 1C1G，源码部署会触发：

前端 pnpm build
TypeScript / Vite 构建
Go 编译
这在你的机器上已经实测会失败，典型现象是：

Node heap OOM
exit code 137
宿主机 OOM killer 直接杀掉 Docker build
所以当前正确策略是：

后端只用官方镜像
前端只用 Vercel
服务器不参与前端构建
3. DNS 配置
先在你的 DNS 提供商里加下面几条记录。

3.1 后端 DNS
新增一条 A 记录：

api.purecodex.asia -> 45.61.135.12
3.2 前端 DNS
把下面两个域名接到 Vercel：

purecodex.asia
www.purecodex.asia
如果你用 Cloudflare：

api.purecodex.asia 可以继续走 Cloudflare 代理
purecodex.asia / www.purecodex.asia 按 Vercel 的域名接入方式配置
4. 服务器部署官方后端镜像
下面这部分就是你已经跑通的成功路径。

4.1 创建部署目录
mkdir -p ~/sub2api-deploy
cd ~/sub2api-deploy
4.2 下载官方部署脚本并初始化
curl -sSL https://raw.githubusercontent.com/Wei-Shaw/sub2api/main/deploy/docker-deploy.sh | bash
这一步会自动生成：

docker-compose.yml
.env
.env.example
data/
postgres_data/
redis_data/
4.3 启动后端
如果系统没有 docker compose，先安装 Docker Compose 插件或兼容命令。

你现在机器上已经可以直接使用：

cd ~/sub2api-deploy
docker compose up -d
查看日志：

cd ~/sub2api-deploy
docker compose logs -f sub2api
4.4 记录首次生成的管理员密码
首次启动时日志会输出：

Generated admin password (one-time): xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
把这串密码保存下来。

如果日志里出现类似下面内容，说明自动初始化已经成功：

Auto setup completed successfully!
Server started on 0.0.0.0:8080
4.5 本机检查后端健康
cd ~/sub2api-deploy
curl http://127.0.0.1:8080/health
再看容器状态：

cd ~/sub2api-deploy
docker compose ps
5. 给后端加反向代理域名
现在容器已经在本机 8080 端口提供服务，下一步是把它挂到：

https://api.purecodex.asia
你可以用 Nginx 或 Caddy。下面给 Nginx 示例。

5.1 安装 Nginx
apt update
apt install -y nginx
5.2 新建站点配置
nano /etc/nginx/sites-available/sub2api-api
写入：

server {
listen 80;
server_name api.purecodex.asia;

client_max_body_size 256m;

location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
}
5.3 启用配置
ln -sf /etc/nginx/sites-available/sub2api-api /etc/nginx/sites-enabled/sub2api-api
nginx -t
systemctl reload nginx
5.4 检查后端域名
curl -I http://api.purecodex.asia/health
curl -I http://api.purecodex.asia/api/v1/settings/public
如果你之后要上 HTTPS，可以继续在 Cloudflare 或 Nginx 证书层完成。

修改后端运行配置
默认官方镜像启动后，跨域和前端地址通常还没配好。你必须补下面两类配置：
前端公网地址
CORS 允许来源
6.1 进入数据目录
官方镜像初始化后，配置文件通常会写到：

cd ~/sub2api-deploy
ls data
你应该能看到：

config.yaml
6.2 编辑 config.yaml
nano ~/sub2api-deploy/data/config.yaml
找到并修改下面内容：

server:
frontend_url: "https://purecodex.asia"

cors:
allowed_origins:
- "https://purecodex.asia"
- "https://www.purecodex.asia"
allow_credentials: true
说明：

frontend_url 用于密码重置邮件、外链回跳等前端地址场景
allowed_origins 必须包含 Vercel 前端域名
你的日志里已经明确提示过：
CORS allowed_origins not configured; cross-origin requests will be rejected
6.3 重启后端容器
cd ~/sub2api-deploy
docker compose restart sub2api
docker compose logs --tail=100 sub2api
7. 在 Vercel 部署前端
这部分在你的本地开发机执行，不在 Ubuntu 服务器上执行。

7.1 安装 Vercel CLI
npm i -g vercel
vercel login
7.2 进入前端目录
cd G:\sub2api\sub2api_purecodex\frontend
7.3 在 Vercel 导入仓库时这样填
必须这样配置：

Root Directory: frontend
Build Command: pnpm build:vercel
Output Directory: dist
Install Command: pnpm install
项目名建议：

sub2api-purecodex-frontend
7.4 配置前端环境变量
Vercel 里添加：

VITE_API_BASE_URL=https://api.purecodex.asia/api/v1
环境选择：

Production
Preview
7.5 本地先验证前端能构建
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
7.6 部署前端
预览部署：

cd G:\sub2api\sub2api_purecodex\frontend
vercel
正式部署：

cd G:\sub2api\sub2api_purecodex\frontend
vercel --prod
8. 首次进入后台后的必改项
当前后端虽然已经跑起来，但后台里还要确认站点配置。

后台入口通常是：

https://purecodex.asia
使用日志里生成的管理员账号密码登录。

8.1 Site / OEM 配置
在后台确认：

api_base_url
填：

https://api.purecodex.asia/api/v1
frontend_url
填：

https://purecodex.asia
8.2 OAuth 回调地址
所有 OAuth 后端回调地址，都应该指向后端域名：

LinuxDo
https://api.purecodex.asia/api/v1/auth/oauth/linuxdo/callback
GitHub
https://api.purecodex.asia/api/v1/auth/oauth/github/callback
Google
https://api.purecodex.asia/api/v1/auth/oauth/google/callback
WeChat
https://api.purecodex.asia/api/v1/auth/oauth/wechat/callback
OIDC
https://api.purecodex.asia/api/v1/auth/oauth/oidc/callback
前端回跳地址则保持前端路径，例如：

/auth/oauth/callback
/auth/wechat/callback
/auth/oidc/callback
9. 上线后验证
9.1 验证后端
curl http://127.0.0.1:8080/health
curl http://127.0.0.1:8080/api/v1/settings/public
curl -I http://api.purecodex.asia/health
curl -I http://api.purecodex.asia/api/v1/settings/public
9.2 验证前端
打开：

https://purecodex.asia
确认：

首页正常打开
登录页正常打开
Network 里 API 请求都发往：
https://api.purecodex.asia/api/v1/...
9.3 验证 CORS
如果前端页面能打开，但登录请求失败并在浏览器控制台出现跨域报错，优先回头检查：

cors:
allowed_origins:
- "https://purecodex.asia"
- "https://www.purecodex.asia"
allow_credentials: true
10. 后续更新流程
10.1 更新后端
这套路径因为使用的是官方镜像，所以更新方式不是重新编译源码，而是拉新镜像。

cd ~/sub2api-deploy
docker compose pull
docker compose up -d
docker compose logs --tail=100 sub2api
10.2 更新前端
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
vercel --prod
11. 常见问题
11.1 为什么官方镜像部署一次成功，而源码部署总失败
因为官方镜像部署：

直接拉现成镜像
不在你的服务器上执行前端构建
不在你的服务器上本地编译 Go
而源码部署会在本机做：

pnpm build
Vite 打包
TypeScript 检查
Go 编译
对 1C1G 机器来说，这条链太重。

11.2 为什么不能继续用本仓库的 docker-compose.build.yml
因为那条路径会尝试本地构建前端镜像，继续触发 OOM。

当前机器不应该再走：

docker compose -f docker-compose.build.yml up -d --build
11.3 如果后端成功但前端报跨域怎么办
先检查：

nano ~/sub2api-deploy/data/config.yaml
确认有：

cors:
allowed_origins:
- "https://purecodex.asia"
- "https://www.purecodex.asia"
allow_credentials: true
然后重启：

cd ~/sub2api-deploy
docker compose restart sub2api
11.4 如果忘了管理员密码怎么办
你当前这套官方镜像是首次启动时一次性打印管理员密码。

如果没保存，优先先确认：

cd ~/sub2api-deploy
docker compose logs sub2api | grep -i password
如果日志里拿不到，再考虑数据库内重置，不要先重装。