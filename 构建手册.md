# Sub2API PureCodex 构建手册

本文档只保留当前最适合 `1C1G` 服务器的上线方案：

- 后端：继续使用官方镜像部署到 Ubuntu 服务器
- 前端：部署到 Vercel
- 不在服务器上做任何源码编译

当前目标域名：

- 前端主域名：`https://purecodex.asia`
- 前端备用域名：`https://www.purecodex.asia`
- 后端域名：`https://api.purecodex.asia`
- 服务器公网 IP：`45.61.135.12`

这份手册的目标是：只走你已经验证成功的路径，不再使用本仓库源码在服务器上构建 Docker 镜像。

---

## 1. 总体架构

最终流量关系：

- 用户访问：`https://purecodex.asia`
- 前端页面：由 Vercel 提供
- 前端 API：请求 `https://api.purecodex.asia/api/v1`
- 后端服务：运行在 Ubuntu 服务器 `45.61.135.12`

一句话总结：

- `purecodex.asia` = Vercel 前端
- `api.purecodex.asia` = Ubuntu 后端

---

## 2. 为什么不用源码编译部署

你的服务器是 `1C1G`，源码部署会触发：

- 前端 `pnpm build`
- TypeScript / Vite 打包
- Go 编译

这在你的机器上已经实测会失败，典型现象是：

- Node heap OOM
- `exit code 137`
- 宿主机 OOM killer 直接杀掉 Docker build

所以当前正确策略是：

- 后端只用官方镜像
- 前端只用 Vercel
- 服务器不参与前端构建

---

## 3. DNS 配置

先在你的 DNS 提供商里加下面几条记录。

### 3.1 后端 DNS

新增一条 `A` 记录：

```text
api.purecodex.asia -> 45.61.135.12
```

### 3.2 前端 DNS

把下面两个域名接到 Vercel：

- `purecodex.asia`
- `www.purecodex.asia`

如果你用 Cloudflare：

- `api.purecodex.asia` 可以继续走 Cloudflare 代理（橙云）
- `purecodex.asia` / `www.purecodex.asia` 按 Vercel 的域名接入方式配置

---

## 4. 服务器部署官方后端镜像

下面这部分就是你已经跑通的成功路径。

### 4.1 创建部署目录

```bash
mkdir -p ~/sub2api-deploy
cd ~/sub2api-deploy
```

### 4.2 下载官方部署脚本并初始化

```bash
curl -sSL https://raw.githubusercontent.com/Wei-Shaw/sub2api/main/deploy/docker-deploy.sh | bash
```

这一步会自动生成：

- `docker-compose.yml`
- `.env`
- `.env.example`
- `data/`
- `postgres_data/`
- `redis_data/`

### 4.3 启动后端

你现在机器上已经可以直接使用：

```bash
cd ~/sub2api-deploy
docker compose up -d
```

查看日志：

```bash
cd ~/sub2api-deploy
docker compose logs -f sub2api
```

### 4.4 记录首次生成的管理员密码

首次启动时日志会输出：

```text
Generated admin password (one-time): xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

把这串密码保存下来。

如果日志里出现类似下面内容，说明自动初始化已经成功：

```text
Auto setup completed successfully!
Server started on 0.0.0.0:8080
```

### 4.5 本机检查后端健康

```bash
cd ~/sub2api-deploy
curl http://127.0.0.1:8080/health
curl http://127.0.0.1:8080/api/v1/settings/public
docker compose ps
```

---

## 5. 给后端加反向代理域名

现在容器已经在本机 `8080` 端口提供服务，下一步是把它挂到：

```text
https://api.purecodex.asia
```

这里先用 Nginx。

### 5.1 安装 Nginx

```bash
apt update
apt install -y nginx
```

### 5.2 新建站点配置

```bash
nano /etc/nginx/sites-available/sub2api-api
```

先写 HTTP 版本：

```nginx
server {
    listen 80;
    server_name api.purecodex.asia;

    client_max_body_size 256m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### 5.3 启用配置

```bash
ln -sf /etc/nginx/sites-available/sub2api-api /etc/nginx/sites-enabled/sub2api-api
nginx -t
systemctl reload nginx
```

### 5.4 检查后端域名

```bash
curl http://api.purecodex.asia/api/v1/settings/public
```

如果这一步返回 JSON，说明：

- DNS 正常
- Nginx 80 正常
- 容器服务正常

---

## 6. 修改后端运行配置

默认官方镜像启动后，跨域和前端地址通常还没配好。你必须补下面两类配置：

- 前端公网地址
- CORS 允许来源

### 6.1 配置文件在哪里

官方镜像初始化后，配置文件通常会写到：

```bash
~/sub2api-deploy/data/config.yaml
```

确认：

```bash
cd ~/sub2api-deploy
ls data
cat data/config.yaml
```

### 6.2 编辑 `config.yaml`

```bash
nano ~/sub2api-deploy/data/config.yaml
```

至少保证有下面内容：

```yaml
server:
  host: 0.0.0.0
  port: 8080
  mode: release
  frontend_url: "https://purecodex.asia"

cors:
  allowed_origins:
    - "https://purecodex.asia"
    - "https://www.purecodex.asia"
  allow_credentials: true
```

说明：

- `frontend_url` 用于密码重置邮件、外链回跳等前端地址场景
- `allowed_origins` 必须包含 Vercel 前端域名
- 你的日志里已经明确提示过：

```text
CORS allowed_origins not configured; cross-origin requests will be rejected
```

### 6.3 重启后端容器

```bash
cd ~/sub2api-deploy
docker compose restart sub2api
docker compose logs --tail=100 sub2api
```

---

## 7. Cloudflare HTTPS 方案选择

你当前 `api.purecodex.asia` 在 Cloudflare 之下，有两种方案。

### 7.1 方案 A：先用 `Flexible`

适合场景：

- 你想先尽快让前端登录恢复
- 源站暂时还没配置 443
- 先求可用

链路是：

- 浏览器 -> Cloudflare：HTTPS
- Cloudflare -> 源站：HTTP

优点：

- 配置最快
- 适合应急恢复

缺点：

- 不是端到端 HTTPS
- 不适合长期正式生产

Cloudflare 设置：

- `SSL/TLS`
- `Overview`
- 选择：

```text
Flexible
```

验证：

```bash
curl https://api.purecodex.asia/api/v1/settings/public
```

如果这里通了，前端就能通过 HTTPS 调到 Cloudflare，再由 Cloudflare 回源到你的 HTTP 源站。

### 7.2 方案 B：正式方案 `Full (strict)`

适合场景：

- 长期正式运行
- Cloudflare 到源站也要求 HTTPS
- 登录、OAuth、管理后台、支付等敏感接口不想长期跑明文回源

推荐策略：

- 短期先用 `Flexible`
- 网站功能全通后，尽快切到 `Full (strict)`

---

## 8. 给源站补 443，并切到 `Full (strict)`

### 8.1 在 Cloudflare 生成 Origin CA 证书

Cloudflare 面板路径：

- `SSL/TLS`
- `Origin Server`
- `Create Certificate`

建议 Hostname：

```text
api.purecodex.asia
```

生成后会拿到两段内容：

- Origin Certificate
- Private Key

### 8.2 服务器保存证书

```bash
mkdir -p /etc/nginx/cloudflare-origin
nano /etc/nginx/cloudflare-origin/api.purecodex.asia.crt
nano /etc/nginx/cloudflare-origin/api.purecodex.asia.key
chmod 600 /etc/nginx/cloudflare-origin/api.purecodex.asia.key
chmod 644 /etc/nginx/cloudflare-origin/api.purecodex.asia.crt
```

### 8.3 修改 Nginx 配置为 80+443

编辑：

```bash
nano /etc/nginx/sites-available/sub2api-api
```

改成：

```nginx
server {
    listen 80;
    server_name api.purecodex.asia;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.purecodex.asia;

    ssl_certificate /etc/nginx/cloudflare-origin/api.purecodex.asia.crt;
    ssl_certificate_key /etc/nginx/cloudflare-origin/api.purecodex.asia.key;

    client_max_body_size 256m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### 8.4 重载 Nginx

```bash
nginx -t
systemctl reload nginx
```

### 8.5 先验证源站本机 443

因为 `Origin CA` 不是公网浏览器证书链，源站本机测试建议这样：

```bash
curl -k --resolve api.purecodex.asia:443:127.0.0.1 https://api.purecodex.asia/api/v1/settings/public
```

如果这里返回 JSON，说明源站 443 已正常。

### 8.6 Cloudflare 切到 `Full (strict)`

Cloudflare 面板路径：

- `SSL/TLS`
- `Overview`
- 选择：

```text
Full (strict)
```

### 8.7 公网验证

```bash
curl https://api.purecodex.asia/api/v1/settings/public
```

如果这里返回 JSON，说明正式 HTTPS 链路已经完成。

---

## 9. 在 Vercel 部署前端

这部分在你的本地开发机执行，不在 Ubuntu 服务器上执行。

### 9.1 Vercel CLI 有什么用

`vercel login` / `Vercel CLI` 不是必须。

如果你已经在 Vercel 网页里完成：

- 导入仓库
- 配环境变量
- 点 Deploy

那完全可以不用 CLI。

CLI 主要用于：

- 本地命令行直接部署
- 拉取环境变量
- 查看部署/日志

如果你习惯网页流，CLI 可以先不装。

### 9.2 安装 Vercel CLI（可选）

```bash
npm i -g vercel
vercel login
```

### 9.3 进入前端目录

```bash
cd G:\sub2api\sub2api_purecodex\frontend
```

### 9.4 在 Vercel 导入仓库时这样填

必须这样配置：

- Root Directory: `frontend`
- Build Command: `pnpm build:vercel`
- Output Directory: `dist`
- Install Command: `pnpm install`

项目名建议：

```text
sub2api-purecodex-frontend
```

### 9.5 配置前端环境变量

Vercel 里添加：

```text
VITE_API_BASE_URL=https://api.purecodex.asia/api/v1
```

环境选择：

- `Production`
- `Preview`

### 9.6 本地先验证前端构建

```bash
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
```

### 9.7 部署前端

预览部署：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel
```

正式部署：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel --prod
```

---

## 10. 首次进入后台后的必改项

后台入口通常是：

```text
https://purecodex.asia
```

使用日志里生成或重置后的管理员密码登录。

### 10.1 Site / OEM 配置

在后台确认：

- `api_base_url`

填：

```text
https://api.purecodex.asia/api/v1
```

- `frontend_url`

填：

```text
https://purecodex.asia
```

### 10.2 OAuth 回调地址

所有 OAuth 后端回调地址，都应该指向后端域名：

- LinuxDo

```text
https://api.purecodex.asia/api/v1/auth/oauth/linuxdo/callback
```

- GitHub

```text
https://api.purecodex.asia/api/v1/auth/oauth/github/callback
```

- Google

```text
https://api.purecodex.asia/api/v1/auth/oauth/google/callback
```

- WeChat

```text
https://api.purecodex.asia/api/v1/auth/oauth/wechat/callback
```

- OIDC

```text
https://api.purecodex.asia/api/v1/auth/oauth/oidc/callback
```

前端回跳地址保持前端路径，例如：

```text
/auth/oauth/callback
/auth/wechat/callback
/auth/oidc/callback
```

---

## 11. 上线后验证

### 11.1 验证后端

```bash
curl http://127.0.0.1:8080/api/v1/settings/public
curl http://api.purecodex.asia/api/v1/settings/public
curl https://api.purecodex.asia/api/v1/settings/public
```

### 11.2 验证前端

打开：

```text
https://purecodex.asia
```

确认：

- 首页正常打开
- 登录页正常打开
- Network 里 API 请求都发往：

```text
https://api.purecodex.asia/api/v1/...
```

### 11.3 前端 `Network error. Please check your connection.`

这个错误通常不是“密码错了”，而是下面几类问题：

- `VITE_API_BASE_URL` 写错
- `api.purecodex.asia` 的 HTTPS 没通
- Cloudflare `521`
- 后端 `CORS` 没配好

按顺序检查：

1. Vercel 环境变量：

```text
VITE_API_BASE_URL=https://api.purecodex.asia/api/v1
```

2. 源站 HTTP：

```bash
curl http://127.0.0.1:8080/api/v1/settings/public
curl http://api.purecodex.asia/api/v1/settings/public
```

3. 公网 HTTPS：

```bash
curl https://api.purecodex.asia/api/v1/settings/public
```

如果第 1、2 正常，而第 3 报：

```text
error code: 521
```

说明不是前端代码或账号密码问题，而是 Cloudflare 到源站的 `443` 没打通。

### 11.4 前端白屏，控制台报 `Cannot access ... before initialization`

这通常是前端模块初始化阶段的循环依赖问题，不是后端密码、CORS 或 HTTPS 问题。

如果你已经更新到本仓库当前版本，并重新部署了 Vercel 前端，这个问题应已修复。

重新部署前端：

```bash
cd G:\sub2api\sub2api_purecodex\frontend
vercel --prod
```

---

## 12. 忘记管理员密码怎么办

### 12.1 查看日志里是否还有一次性密码

```bash
cd ~/sub2api-deploy
docker compose logs sub2api | grep -i password
```

如果日志里拿不到，再走数据库重置。

### 12.2 进入 PostgreSQL

```bash
cd ~/sub2api-deploy
docker compose exec postgres psql -U sub2api -d sub2api
```

### 12.3 查看管理员账号

```sql
select id, email, role, status from users order by id;
```

通常管理员邮箱是：

```text
admin@sub2api.local
```

### 12.4 直接重置管理员密码

在 `psql` 里执行：

```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;
UPDATE users
SET password_hash = crypt('你的新密码', gen_salt('bf')),
    updated_at = now()
WHERE email = 'admin@sub2api.local';
```

验证哈希前缀：

```sql
SELECT email, left(password_hash, 4) AS prefix
FROM users
WHERE email = 'admin@sub2api.local';
```

如果看到：

```text
$2a$
```

说明 bcrypt 哈希已经更新。

退出：

```sql
\q
```

### 12.5 本机验证新密码

```bash
curl -s http://127.0.0.1:8080/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@sub2api.local","password":"你的新密码"}'
```

如果返回 `access_token`，说明密码已生效。

---

## 13. 后续更新流程

### 13.1 更新后端

这套路径因为使用的是官方镜像，所以更新方式不是重新编译源码，而是拉新镜像。

```bash
cd ~/sub2api-deploy
docker compose pull
docker compose up -d
docker compose logs --tail=100 sub2api
```

### 13.2 更新前端

```bash
cd G:\sub2api\sub2api_purecodex\frontend
pnpm install
pnpm typecheck
pnpm build:vercel
vercel --prod
```

---

## 14. 常见问题

### 14.1 为什么官方镜像部署一次成功，而源码部署总失败

因为官方镜像部署：

- 直接拉现成镜像
- 不在你的服务器上执行前端构建
- 不在你的服务器上本地编译 Go

而源码部署会在本机做：

- `pnpm build`
- Vite 打包
- TypeScript 检查
- Go 编译

对 `1C1G` 机器来说，这条链太重。

### 14.2 为什么不能继续用本仓库的源码构建 compose

因为那条路径会尝试本地构建前端镜像，继续触发 OOM。

当前机器不应该再走：

```bash
docker compose -f docker-compose.build.yml up -d --build
```

### 14.3 只用 `Flexible` 会怎么样

可以短期使用，但不建议长期停留。

`Flexible` 的实际链路是：

- 浏览器 -> Cloudflare：HTTPS
- Cloudflare -> 你的源站：HTTP

这意味着：

- 短期恢复登录很合适
- 但不是端到端 HTTPS
- 长期不如 `Full (strict)` 安全

推荐策略：

- 短期：先用 `Flexible`
- 长期：补源站 443，然后切 `Full (strict)`

