# Sub2API PureCodex 构建手册

## 1. 远端首次运行

```bash
git clone https://github.com/AMtanks/sub2api_purecodex.git /opt/sub2api_purecodex
cd /opt/sub2api_purecodex

export PRIMARY_DOMAIN="purecodex.asia"
export EXTRA_SERVER_NAMES="www.purecodex.asia"
export TAILSCALE_AUTHKEY="Y3oJUAxh6gZ4w9"
export SUB2API_ADMIN_PASSWORD="Y3oJUAxh6gZ4w9"

bash deploy/remote-first-run.sh
```

说明：

- 自动安装 Docker、Nginx、Tailscale
- 自动拉起 `docker-compose.build.yml`
- 默认应用只监听 `127.0.0.1:8080`
- Nginx 负责反代到公网

## 2. Cloudflare Origin CA 补装

```bash
cd /opt/sub2api_purecodex
mkdir -p deploy/certs

# put files here:
# deploy/certs/cloudflare-origin.crt
# deploy/certs/cloudflare-origin.key

export PRIMARY_DOMAIN="your-domain.com"
export EXTRA_SERVER_NAMES="www.your-domain.com"

bash deploy/remote-first-run.sh
```

说明：

- 证书文件放好后重跑脚本
- 脚本会把 Nginx 从 HTTP 切到 HTTPS
- Cloudflare 侧使用 `Full (strict)`

## 3. 后续代码更新重部署

```bash
cd /opt/sub2api_purecodex
git pull
cd deploy
docker compose -f docker-compose.build.yml up -d --build
```

说明：

- 保留 `deploy/.env`
- 保留 `deploy/data`
- 保留 `deploy/postgres_data`
- 保留 `deploy/redis_data`

## 4. 基本检查

```bash
docker ps
curl http://127.0.0.1:8080/health
systemctl status nginx --no-pager
tailscale status
```

说明：

- 应用健康检查走 `127.0.0.1:8080`
- 外网访问走 Nginx
